{
  "name": "3d-runner-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "start": "vite --open",
    "build": "vite build",
    "build:pages": "vite build && node scripts/copy-pages.js",
    "preview": "vite preview --open",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.177.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.3.5"
  }
}
